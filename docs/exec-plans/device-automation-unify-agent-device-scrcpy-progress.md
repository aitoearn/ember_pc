# 端自动化统一：agent-device + scrcpy — 进度日志

> 关联计划：`internal/exec-plans/device-automation-unify-agent-device-scrcpy-plan.md`  
> 更新时间：2026-06-15

## 总览

| 阶段 | 状态 | 说明 |
| --- | --- | --- |
| P0 范围冻结与守卫 | `进行中` | 计划入仓；工程导航已改为 agent-device + native scrcpy current 主链，feature flag/tech-debt 登记待收口 |
| P1 daemon 长连 | `进行中` | 已接入 adb track-devices 事件刷新；列表已改为 aya 风格 ADB 快照优先；截图/返回/主页/tap/swipe 优先走 agent-device HTTP daemon |
| P2 agent-device scrcpy | `进行中` | sibling 仓库已新增 scrcpy reverse/start 最小 daemon 命令；reverse 可绑定 Electron socket bridge 端口；真实设备联调待做 |
| P3 Ember 切换 backend | `进行中` | 已补 Electron/Renderer scrcpy reverse/start API 面、窄 TCP socket bridge、Renderer native video/control client 最小链路 |
| P4 Genie 处置 | `进行中` | 已冻结 UI Genie，不再预热或提交 AutoGLM sidecar；后续待 App Server Agent 新计划承接 |
| P5 Release 打包 | `进行中` | 已接 Forge extraResource、资源 manifest/verify 卡点、scrcpy server/adb 下载脚本与 agent-device 最小运行时暂存；临时真实资源 staging 已验收，Forge package 端到端待跑 |
| P6 清理收口 | `进行中` | 工程导航已更新；AutoGLM control/screenshot dead helper 已删除，Genie task 历史入口暂留待后续计划 |

**主线完成度：** 84%（计划已写，已完成设备发现事件刷新、aya 风格 Android 列表快路径、截图/触控 daemon 热路径、scrcpy bootstrap 最小命令、Ember IPC API 面、主进程 socket bridge、Renderer native scrcpy video/control 最小链路、UI Genie 冻结、截图/控制去 AutoGLM 分支、P5 资源 staging/verify 卡点、scrcpy/adb 下载入口、agent-device 最小运行时暂存、临时真实资源 staging 验收、renderer build、工程导航事实源更新与 AutoGLM control/screenshot dead helper 清理）  
**含 Genie 替代的整体完成度：** 0%

## 进度条目（append-only）

### 2026-06-15

- 创建执行计划与 progress 文件。
- 背景：AutoGLM 仅承担 scrcpy + control + Genie；用户确认不含 Phone Agent 时可由 agent-device + scrcpy 替代 sidecar。
- 待定：P4 选 A（冻结 Genie）还是 B（App Server Agent）。
- 按用户要求参考 `/Users/lisq/ai/testplatform/aya`：确认其方案为主进程 `adb reverse + push scrcpy.jar + app_process`，Renderer 直接用 `@yume-chan/scrcpy` 解析 video/audio/control socket；计划已从「agent-device Socket.IO 媒体转发」修正为「agent-device bootstrap + Renderer native scrcpy client」。
- 补充设备发现参考：`aya` 使用长期 `@devicefarmer/adbkit` client + `trackDevices()` 监听 `add/remove`，2s debounce 后广播 `changeDevice`，Renderer 再刷新 `getDevices()`；计划 P1 已补 Android watcher、平台 fast path 与自动刷新退出条件。
- 完成 P1 设备发现事件刷新最小切片：新增 `DeviceInventoryWatcher`（`adb track-devices` + 2s debounce + 自动重连）、`device_automation_inventory_changed` 事件、前端 `listenDeviceAutomationInventoryChanged` 与 Workspace 自动刷新订阅。
- 验证：`npm exec vitest run src/features/device-automation/DeviceAutomationWorkspace.test.tsx electron/deviceAutomation/deviceInventoryWatcher.test.ts` 通过；`npm run typecheck:electron` 通过。`npx tsc --noEmit` 仍失败于既有 `DeviceAutomationListPage.tsx` 三个类型错误（非本切片新增）。
- 完成 P1 列表 daemon 热路径切片：新增 `agentDeviceDaemonClient`，读取 `daemon.json` 后通过 `/health` + `/rpc` 调 `agent_device.command devices`；作为 agent-device 补充与兜底路径，失败再回退 CLI。首次无 daemon 时仍需 CLI 启动 daemon，后续刷新避免重复 spawn。
- 验证：`npm exec vitest run electron/deviceAutomation/agentDeviceDaemonClient.test.ts electron/deviceAutomation/deviceInventoryWatcher.test.ts src/features/device-automation/DeviceAutomationWorkspace.test.tsx` 通过；`npm run typecheck:electron` 通过；本机已存在 agent-device `daemon.json`（HTTP port 可读）。
- 修正为 aya 风格 Android 列表 fast path：`DeviceInventoryWatcher` 维护 `adb track-devices` 在线设备快照，`DeviceAutomationRuntime.listDevices` 优先使用 watcher 的 Android 快照，并用 agent-device daemon/CLI 结果补充非 Android 设备。这样 Android 列表不再依赖每次 `agent-device devices` 发现，daemon 只做跨平台补充与兜底。
- 验证：`npm exec vitest run electron/deviceAutomation/runtime.test.ts electron/deviceAutomation/agentDeviceDaemonClient.test.ts electron/deviceAutomation/deviceInventoryWatcher.test.ts src/features/device-automation/DeviceAutomationWorkspace.test.tsx` 通过；`npm run typecheck:electron` 通过。
- 完成 P1 截图/触控 daemon 热路径：`agentDeviceDaemonClient` 增加 `screenshot`、`back/home`、`press`、`swipe` JSON-RPC 封装；`DeviceAutomationRuntime` 在 AutoGLM 截图不可用或未启用 AutoGLM 控制时，优先通过 agent-device HTTP daemon 执行，失败再回退原 CLI。这样进入调试页后的截图、返回/主页、tap、swipe 不再默认每次 `spawnSync`。
- 验证：`npm exec vitest run electron/deviceAutomation/runtime.test.ts electron/deviceAutomation/agentDeviceDaemonClient.test.ts electron/deviceAutomation/deviceInventoryWatcher.test.ts src/features/device-automation/DeviceAutomationWorkspace.test.tsx` 通过；`npm run typecheck:electron` 通过；相关文件 lint 无新增错误。
- 完成 P2 scrcpy bootstrap 最小切片（sibling `agent-device`）：新增内部 daemon commands `scrcpy_reverse_tcp` / `scrcpy_start` 与 `scrcpy` handler route；`scrcpy_reverse_tcp` 可为 `localabstract:scrcpy_<scid>` 分配/复用本地 `tcp:<port>` reverse；`scrcpy_start` 支持 push `scrcpy.jar` 到 `/data/local/tmp/ember/scrcpy.jar` 并后台执行 `app_process ... com.genymobile.scrcpy.Server 3.1 ...`。暂未扩公开 CLI/Node contract，Ember 通过 daemon JSON-RPC command 调用。
- Ember 侧 `agentDeviceDaemonClient` 增加 `reverseAgentDeviceScrcpyTcpViaDaemon` / `startAgentDeviceScrcpyViaDaemon` 封装，为 P3 Renderer native scrcpy client 接入预留。
- 验证：`cd /Users/lisq/ai/testplatform/agent-device && pnpm exec vitest run src/daemon/handlers/__tests__/scrcpy.test.ts` 通过；`cd /Users/lisq/ai/testplatform/agent-device && pnpm typecheck` 通过；`npm exec vitest run electron/deviceAutomation/agentDeviceDaemonClient.test.ts electron/deviceAutomation/runtime.test.ts electron/deviceAutomation/deviceInventoryWatcher.test.ts src/features/device-automation/DeviceAutomationWorkspace.test.tsx` 通过；`npm run typecheck:electron` 通过。
- 完成 P3 IPC API 面最小切片：`DeviceAutomationRuntime` 增加 `reverseScrcpyTcp` / `startScrcpy`；`hostCommands` 新增 `device_automation_scrcpy_reverse_tcp` / `device_automation_scrcpy_start`；`ipcChannels` 白名单、`src/lib/api/deviceAutomation.ts` 与 `types.ts` 增加对应 Renderer API。当前只是打通调用面，`DeviceScrcpyPlayer` 尚未切为 aya native client。
- 验证：`npm exec vitest run electron/deviceAutomation/runtime.test.ts electron/deviceAutomation/agentDeviceDaemonClient.test.ts` 通过；`npm run typecheck:electron` 通过；相关文件 lint 无新增错误。
- 完成 P3 native scrcpy socket bridge 切片：新增 `DeviceScrcpySocketBridge`，由 Electron 主进程创建仅供 scrcpy 使用的本地 TCP server，通过 `device_automation_scrcpy_socket_connect/data/close` 事件向 Renderer 发送 socket 生命周期和 base64 数据，并通过 `device_automation_scrcpy_socket_write/close/create` IPC 提供写入与清理。此设计避免在 Renderer 暴露通用 `net` 能力，符合计划中的安全边界。
- Renderer API 增加 `createDeviceAutomationScrcpySocketServer` / `writeDeviceAutomationScrcpySocket` / `closeDeviceAutomationScrcpySocketServer` 与三类 socket event listener；`commandPolicy` 将 `device_automation_scrcpy_socket_` 事件列为 bridge truth。
- 验证：`npm exec vitest run electron/deviceAutomation/deviceScrcpySocketBridge.test.ts electron/deviceAutomation/runtime.test.ts electron/deviceAutomation/agentDeviceDaemonClient.test.ts` 通过；`npm run typecheck:electron` 通过；相关文件 lint 无新增错误。
- 完成 P3 Renderer native scrcpy 最小链路：新增 `AgentDeviceScrcpySocketTransport`，把主进程 socket bridge 事件转换为 per-socket `ReadableStream` / `WritableStream`；`DeviceScrcpyPlayer` 不再依赖 AutoGLM `baseUrl` 或 Socket.IO，Android 调试页直接创建 Electron TCP server、把本地端口传给 `agent-device scrcpy_reverse_tcp`、启动 `scrcpy_start`，并用 `ScrcpyOptions3_1` + `WebCodecsVideoDecoder` 渲染 video socket。
- 补齐 scrcpy control 最小链路：`DeviceScrcpyPlayer` 等待 socket 1 后创建 `ScrcpyControlMessageWriter`，scrcpy 模式下 pointer down/move/up 优先写原生 control socket；control 未就绪时仍回退 agent-device `press/swipe`。Android 调试页 scrcpy 展示条件已从 AutoGLM sidecar ready 改为 Android 在线设备即可进入。
- 修正 reverse 端口语义：Ember 创建的 Electron socket server port 会作为 `localPort` 传给 `agent-device`，sibling `agent-device` `scrcpy_reverse_tcp` 现在支持使用该端口生成 `tcp:<localPort>` reverse，避免自行分配端口导致设备端连接不到 Electron bridge。
- `scrcpy.jar` 路径解析下沉到 Electron runtime：Renderer 不再传 `scrcpyServerPath`；host 优先读 `DEVICE_AUTOMATION_SCRCPY_SERVER_PATH`，否则查 `process.resourcesPath/device-automation/scrcpy.jar` 与开发态资源候选。P5 仍需补下载与 Forge extraResource。
- 验证：Ember `npm exec vitest run electron/deviceAutomation/agentDeviceDaemonClient.test.ts electron/deviceAutomation/runtime.test.ts src/features/device-automation/scrcpy/AgentDeviceScrcpySocketTransport.test.ts electron/deviceAutomation/deviceScrcpySocketBridge.test.ts` 通过；`npm run typecheck:electron` 通过；`npx tsc --noEmit --pretty false --skipLibCheck` 仍仅失败于既有 `DeviceAutomationListPage.tsx` 三个类型错误。sibling `agent-device` `pnpm test:unit src/daemon/handlers/__tests__/scrcpy.test.ts` 与 `pnpm typecheck` 通过。
- 推进 P5 打包资源第一刀：新增 `scripts/device-automation/stage-device-automation-resources.mjs`，将可用的 `scrcpy.jar`、adb 目录和 agent-device build 输出暂存到 `dist-electron/device-automation` 并写 `manifest.json`；`electron:build:host` 会串行执行该 staging 脚本，`forge.config.mjs` 将 `dist-electron/device-automation` 加入 Forge `extraResource`。
- Electron 资源解析补强：`resolveToolRoot` 增加 packaged resource candidate，agent-device packaged 路径为 `process.resourcesPath/device-automation/agent-device`；Android inventory watcher 也会优先解析 `process.resourcesPath/device-automation/adb` 或 `DEVICE_AUTOMATION_ADB_DIR`。
- Package verify 增加设备自动化资源卡点：`verify-package-resources.mjs` 要求 packaged resource root 中存在 `device-automation/manifest.json` 且 `scrcpyServer` 已 staged，并断言 `scrcpy.jar` 存在。当前只强制 `scrcpyServer`，adb 与 agent-device 依赖完整性仍待 P5 下一刀收口。
- 验证：`npm run electron:build:device-automation-assets` 可运行并明确报告本机缺少 `scrcpy.jar`/adb；`npm exec vitest run scripts/electron/verify-package-resources.test.mjs scripts/electron/forge-config.test.mjs` 通过；`npm run typecheck:electron` 通过。
- 补 P5 scrcpy server 下载入口：新增 `scripts/device-automation/download-scrcpy-server.mjs`，默认下载 Genymobile `scrcpy-server-v3.1` 到 `resources/device-automation/scrcpy.jar`，支持 `--version`、`--output`、`DEVICE_AUTOMATION_SCRCPY_VERSION` 和 `DEVICE_AUTOMATION_SCRCPY_SERVER_PATH`；新增 npm script `electron:download:scrcpy-server`。此入口只处理 scrcpy server jar，不绑定 AutoGLM。
- 验证：`npm exec vitest run scripts/device-automation/download-scrcpy-server.test.mjs scripts/electron/verify-package-resources.test.mjs scripts/electron/forge-config.test.mjs` 通过；`npm run typecheck:electron` 通过。
- 补 P5 adb 下载入口：新增 `scripts/device-automation/download-adb-platform-tools.mjs`，按平台下载 Google 官方 `platform-tools-latest-{darwin|windows|linux}.zip`，解出当前平台需要的 adb 文件到 `resources/device-automation/adb`；新增 npm script `electron:download:adb`。该资源会被 staging 脚本复制到 `dist-electron/device-automation/adb`，packaged runtime 通过 `process.resourcesPath/device-automation/adb` 解析。
- 验证：`npm exec vitest run scripts/device-automation/download-adb-platform-tools.test.mjs scripts/device-automation/download-scrcpy-server.test.mjs scripts/electron/verify-package-resources.test.mjs scripts/electron/forge-config.test.mjs` 通过；`npm run typecheck:electron` 通过。
- 收口 P5 agent-device 最小运行时暂存：`stage-device-automation-resources.mjs` 现在会复制 sibling `agent-device` 的 `dist`、`bin`、`package.json` 和唯一运行时依赖 `node_modules/yaml` 到 `dist-electron/device-automation/agent-device`；manifest 记录 `dependencies: ["yaml"]`。`verify-package-resources.mjs` 增加 agent-device 卡点，检查 packaged `package.json`、`bin/agent-device.mjs`、`dist/src/internal/bin.js` 与 `node_modules/yaml/package.json`。
- 验证：本机运行 `npm run electron:build:device-automation-assets` 后，manifest 已显示 `agentDevice.status=staged`，仍缺 `scrcpy.jar` 与 adb（需执行下载脚本或设置资源路径）；`npm exec vitest run scripts/electron/verify-package-resources.test.mjs scripts/device-automation/download-adb-platform-tools.test.mjs scripts/device-automation/download-scrcpy-server.test.mjs` 通过；`npm run typecheck:electron` 通过。
- 完成 P5 临时真实资源 staging 验收：已用 `download-scrcpy-server.mjs --output .tmp/device-automation/scrcpy.jar` 下载 scrcpy server，用 `download-adb-platform-tools.mjs --output .tmp/device-automation/adb` 下载当前平台 adb；随后通过 `DEVICE_AUTOMATION_SCRCPY_SERVER_PATH=.tmp/device-automation/scrcpy.jar DEVICE_AUTOMATION_ADB_DIR=.tmp/device-automation/adb npm run electron:build:device-automation-assets` 暂存资源。虽然命令被用户中断在收尾阶段，但 `dist-electron/device-automation/manifest.json` 已写出且 `scrcpyServer`、`adb`、`agentDevice` 均为 `staged`。
- 暂存资源验证：`verifyDeviceAutomationResources('dist-electron')` 通过；`node dist-electron/device-automation/agent-device/bin/agent-device.mjs --help` 能成功启动并输出 help；资源相关测试 `npm exec vitest run scripts/electron/verify-package-resources.test.mjs scripts/device-automation/download-adb-platform-tools.test.mjs scripts/device-automation/download-scrcpy-server.test.mjs scripts/electron/forge-config.test.mjs` 通过；`npm run typecheck:electron` 通过。
- 修复历史 `DeviceAutomationListPage.tsx` 类型阻塞：状态 badge label key 改为 `deviceAutomation.status.${DeviceAutomationStatus}` 模板字面量类型，分页按钮 `size="icon-sm"` 改为现有 Button variant `icon`。`npm run build:renderer:electron:smoke` 已通过，说明此前全量 renderer build 的三个 TS 错误已解除。
- 完成 P4 UI Genie 冻结第一刀：`useDeviceAiTask` 不再 warm/prepare/submit/poll/cancel AutoGLM task session，Android 调试页进入后不会调用 `ensureDeviceAutomationAiSidecar()`；`DeviceAutomationGeniePanel` 展示迁移中/暂不可用文案并禁用自然语言输入，避免用户交互重新拉起 AutoGLM sidecar。
- UI Genie 冻结文案已同步 Ember current 五语言 `zh-CN / zh-TW / en-US / ja-JP / ko-KR`。当前选择是“冻结并标注迁移中”，不是恢复 AutoGLM Phone Agent；后续若保留 Genie，需要另接 App Server / RuntimeCore Agent。
- 验证：`npm exec vitest run src/features/device-automation/hooks/useDeviceAiTask.test.tsx` 通过；`npm run typecheck:electron` 通过；相关文件 lint 无新增错误。
- 继续去 AutoGLM runtime 控制残留：`DeviceAutomationRuntime.captureScreenshot` 不再尝试 `tryCaptureAutoGlmScreenshot`；`sendNavigation` / `sendTap` / `sendSwipe` 忽略旧 `useAutoGlm` 参数并只走 agent-device daemon/CLI。前端 `DeviceAutomationDebugPage` 与 `DeviceScreenshotMirror` 已移除 `sidecar.ready -> useAutoGlm` 分支，截图 fallback、导航和触控均保持 agent-device 主链。
- 验证：`npm exec vitest run electron/deviceAutomation/runtime.test.ts src/features/device-automation/hooks/useDeviceAiTask.test.tsx src/features/device-automation/DeviceAutomationWorkspace.test.tsx` 通过；`npm run typecheck:electron` 通过；相关文件 lint 无新增错误。
- 修复两个新测试的 React act 环境标志类型写法：`globalThis.IS_REACT_ACT_ENVIRONMENT` 改为仓库既有局部类型断言模式，解决完整 `build:renderer` 的 TS7017 阻塞。验证：`npm run build:renderer` 通过，两个测试文件 lint 无新增错误。
- 推进 P0/P6 文档事实源更新：`internal/aiprompts/device-automation.md` 已从旧“双后端 / AutoGLM scrcpy”叙述改为 current 的 `agent-device daemon + Renderer native scrcpy + Electron socket bridge` 主链；命令表、环境变量、本地联调步骤、调试页行为、交付进度和已知限制均同步到 Genie 冻结与 P5 package-dir 验收中的状态。
- 清理 AutoGLM 设备控制 dead helper：删除 `autoGlmApi.ts` 中已零调用的 `sendAutoGlmTap` / `sendAutoGlmSwipe` / `sendAutoGlmNavigation` / `captureAutoGlmScreenshot` / `tryCaptureAutoGlmScreenshot`，并从 runtime 测试 mock 中移除旧断言。保留 task session / task poll / cancel API 作为 Genie 冻结历史入口，待后续 App Server Agent 计划或最终删除。
- 验证：`rg 'sendAutoGlmNavigation|sendAutoGlmTap|sendAutoGlmSwipe|tryCaptureAutoGlmScreenshot|captureAutoGlmScreenshot' electron src` 无匹配；`rg 'useAutoGlm|autoGlmReady' electron src` 无匹配；`npm exec vitest run electron/deviceAutomation/runtime.test.ts` 通过；`npm run typecheck:electron` 通过；相关文件 lint 无新增错误。
- 收口 P5 release 资源 strict gate：`stage-device-automation-resources.mjs` 拆出可测试 `main(argv, env)` 与 `stageDeviceAutomationResources(...)`，支持 `--strict`、`--output`、`--source`、`--scrcpy-server-path`、`--adb-dir`、`--agent-device-root`。默认手动调用仍可写出缺失资源 manifest 便于诊断；`electron:build:device-automation-assets` 改为 `--strict`，缺 `scrcpy.jar` / adb / agent-device runtime 时会失败，避免 release build 继续产出不可用包。
- 验证：`npm exec vitest run scripts/device-automation/stage-device-automation-resources.test.mjs scripts/device-automation/download-adb-platform-tools.test.mjs scripts/device-automation/download-scrcpy-server.test.mjs scripts/electron/verify-package-resources.test.mjs scripts/electron/forge-config.test.mjs` 通过；`npm run typecheck` 通过；`npm run electron:build:device-automation-assets` 在当前未准备资源时按预期失败并提示 `scrcpy.jar` / adb 缺失。
- 完成 P5 本机真实资源 strict staging 验收：`npm run electron:download:scrcpy-server -- --output .tmp/device-automation/scrcpy.jar` 与 `npm run electron:download:adb -- --output .tmp/device-automation/adb` 成功；随后用 `DEVICE_AUTOMATION_SCRCPY_SERVER_PATH=.tmp/device-automation/scrcpy.jar DEVICE_AUTOMATION_ADB_DIR=.tmp/device-automation/adb npm run electron:build:device-automation-assets` 通过，`manifest.json` 中 `scrcpyServer` / `adb` / `agentDevice` 均为 `staged`。
- 验证：`verifyDeviceAutomationResources('dist-electron')` 通过；`node dist-electron/device-automation/agent-device/bin/agent-device.mjs --help` 能从暂存目录启动并输出帮助。P5 剩余缺口是 Forge package-dir / release 安装包端到端冒烟，以及真实 Android 设备 scrcpy 打开链路联调。
- 完成 P5 Forge package-dir 冒烟：带真实资源环境变量运行 `DEVICE_AUTOMATION_SCRCPY_SERVER_PATH=.tmp/device-automation/scrcpy.jar DEVICE_AUTOMATION_ADB_DIR=.tmp/device-automation/adb npm run electron:package:dir` 通过，覆盖 renderer build、Electron host build、app-server assets、device automation strict staging 与 Forge darwin-arm64 目录打包。
- 验证：`node scripts/electron/verify-package-resources.mjs` 通过，packaged resource root 为 `release-electron/Ember-darwin-arm64/Ember.app/Contents/Resources`；packaged `device-automation/manifest.json` 中 `scrcpyServer` / `adb` / `agentDevice` 均为 `staged`；`node release-electron/Ember-darwin-arm64/Ember.app/Contents/Resources/device-automation/agent-device/bin/agent-device.mjs --help` 可启动。P5 剩余缺口收敛为真实 Android 设备 scrcpy 打开链路联调与安装包 make/sign/notarization 级验证。

---
description: "MyWiki 查询工作流（L3 读取）。回答涉及知识库内容的问题时按需触发，定义 Quick/Standard/Deep 三档查询深度与查询链纪律。"
alwaysApply: false
---

# 01 · MyWiki 查询工作流（按需触发）

L3 读取流程。完整步骤见 `.cursor/skills/obsidian-wiki-query/SKILL.md`，本规则给出深度选择与查询链纪律。

## 三档查询深度

| 深度 | 流程 | Token 预算 | 适用 |
| --- | --- | --- | --- |
| **Quick** | `hot.md` → `index.md` → 回答 | ~1,500 | 简单事实、日期、"X 是什么" |
| **Standard** | `hot` → `index` → 3–5 页精读 → 引用回答 | ~3,000 | 多数问题（默认） |
| **Deep** | 全量读取 ≤ 12 页 → 综合 → 保存回 wiki | ~8,000+ | 跨页综合、对比、"把 X 讲全" |

## 查询链纪律

严格按层级下钻，不要一上来就全库 grep：

```
hot.md → index.md → 域_index.md（wiki/<域>/_index.md）→ 具体页面
```

仅在用户明确要求「全文搜索」时，才对整库执行 grep。

## 回答规范

- 每条结论用双链引用来源：`（来源：[[页面名]]）`。
- 命中知识空白（gap）时显式声明「知识库中没有 X」，**不得用训练数据编造**领域内答案。
- 高价值答案询问是否归档到 `wiki/questions/`，归档后补全写后闭环。
